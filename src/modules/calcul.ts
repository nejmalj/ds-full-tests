// mettre un console.log à chaque étape pour vérifier que les pouvoirs sont bien appliqués
export function getJupiterHour (moonHour: {}, sunHour: {}, earthHour: {}) : string {
    // result initialisé à 0

    // calcul des valeurs en checkant si chaque cadran est activé ou pas

    // application des pouvoirs de la lune
    // application des pouvoirs du soleil
    // application des pouvoirs de la terre

    //valeur result finale

    // applique une valeur à jupiterHour (string) selon le resultat obtenu
    return jupiterHour;
}